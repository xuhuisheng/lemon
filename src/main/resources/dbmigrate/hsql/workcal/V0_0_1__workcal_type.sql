

-------------------------------------------------------------------------------
--  workcal type
-------------------------------------------------------------------------------
CREATE TABLE WORKCAL_TYPE(
        ID BIGINT NOT NULL,
	NAME VARCHAR(50),
        CONSTRAINT PK_WORKCAL_TYPE PRIMARY KEY(ID)
);

